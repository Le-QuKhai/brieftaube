name: SIMPLE TEST - Copy ONE File

on:
  push:
    branches:
      - fVMproxy

jobs:
  debug-copy-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: 🕵️ List files in the root directory
        run: ls -la

      - name: 🚚 ATTEMPT TO COPY ONLY docker-compose.yml
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          proxy_host: usershell.informatik.haw-hamburg.de
          proxy_username: infwax193
          proxy_key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "docker-compose.yml" # We are only trying to copy this one file.
          target: "/home/${{ secrets.VM_USERNAME }}/brieftaube_test"